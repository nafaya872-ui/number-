export const CONFIG = {
    // Base path for the application. 
    // If deployed to a subdirectory (like GitHub Pages), set this to the subdirectory name (e.g., '/number-').
    // If deployed to the root domain, leave it as '/'.
    BASE_PATH: window.location.hostname.includes('github.io') ? '/number-' : '/'
};
